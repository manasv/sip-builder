name: Build
on:
   workflow_dispatch:
    paths: 
      - "build-pjsip.sh"
jobs:
  build:
    runs-on: macos-10.15
    steps:
      - uses: actions/checkout@v2
      - name: Build PJSIP
        run: |
          chmod +x build-pjsip.sh
          ./build-pjsip.sh